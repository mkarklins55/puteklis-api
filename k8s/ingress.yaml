apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: puteklis-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: puteklis-api
                port:
                  number: 80
